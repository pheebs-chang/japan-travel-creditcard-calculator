/**
 * 信用卡選卡 UI 實作於 `./card-selector` 的 `CardBadge`：
 * - 卡名為連結，開啟 `officialUrl`（新分頁）
 * - `appearance` 顯示於卡名下方
 * - 版面：p-4、gap-y-2、福利格 min-h-[60px]
 */
export { CardSelector, CardBadge } from "./card-selector";
